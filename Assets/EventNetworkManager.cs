using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
using Photon.Realtime;

public class EventNetworkManager : MonoBehaviourPunCallbacks
{
    private const byte UPDATE_TERRITORY = 0;
    private const byte UPDATE_PLAYER = 1;
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
